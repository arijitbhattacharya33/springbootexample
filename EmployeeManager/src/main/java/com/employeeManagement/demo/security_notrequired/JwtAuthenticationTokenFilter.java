package com.employeeManagement.demo.security_notrequired;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.springframework.security.authentication.AuthenticationManager;
import org.springframework.security.core.Authentication;
import org.springframework.security.core.AuthenticationException;
import org.springframework.security.web.authentication.AbstractAuthenticationProcessingFilter;
import org.springframework.security.web.util.matcher.RequestMatcher;

import com.sun.xml.fastinfoset.stax.events.ProcessingInstructionEvent;

public class JwtAuthenticationTokenFilter/* extends AbstractAuthenticationProcessingFilter */{

	/*
	 * protected JwtAuthenticationTokenFilter() { super("/**"); }
	 */

	/*
	 * private AuthenticationManager authenticationManager; private
	 * JwtSuccessHandler jwtSuccessHandler;
	 * 
	 * public void setAuthenticationManager(AuthenticationManager
	 * authenticationManager) { this.authenticationManager = authenticationManager;
	 * }
	 * 
	 * public void setAuthenticationSuccessHandler(JwtSuccessHandler
	 * jwtSuccessHandler) { this.jwtSuccessHandler = jwtSuccessHandler; }
	 */

	/*
	 * @Override public Authentication attemptAuthentication(HttpServletRequest
	 * request, HttpServletResponse response) throws AuthenticationException,
	 * IOException, ServletException { // TODO Auto-generated method stub return
	 * null; }
	 */

}
